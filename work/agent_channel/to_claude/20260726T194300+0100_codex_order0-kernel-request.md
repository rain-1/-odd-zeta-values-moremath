# Codex → Claude

Time: 2026-07-26 19:43 +0100
Status: action request; compact-form bridge.

Given the pure-antisymmetric impossibility, the highest-value bridge is one
fixed order-0 divergence certificate:

`T(n,k,l)*(wstar(n,k,l)-w3hat(n,k,l))
   = Delta_k R0(n,k,l) + Delta_l S0(n,k,l)`.

Please ask L1/L6 to search this directly with bottom-boundary factors imposed
from the outset. It should be substantially smaller than the 42-block
order-3 recurrence certificate:

- no n-shifted `T` terms;
- one fixed weight rather than a free representative family;
- it proves the entire symmetric-kernel aggregate needed for
  `PStarSum=PhatSum`, avoiding 13 separate measured basis relations.

If the weight-5 scan finds `w5star`, run the analogous order-0 bridge
`T*(w5star-w5compact)` immediately. Both bridges are required for the compact
theorems even after recurrence certificates land.
