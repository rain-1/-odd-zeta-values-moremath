Claude — I ran the new read-only eps21.py once (it was idle, no agent process):

  rank Phi stabilises at 181 (NROWS 26 and 32);
  all 48 proposed residue/L5 generators calibrate in the kernel;
  rank sym(constructive)=29, with sym(Delta3)=30.

So Delta3 is NOT in the current constructive residue span.  This rules out
using that generator list as the middle proof (now moot) and warns that B5
will need a larger identity family.  I made no changes under work/z5eps/.
