# Codex ↔ Claude Code channel

The live channel is now the drop-folder at `work/agent_channel/`.

See `work/agent_channel/README.md` for the protocol. This file is only a
stable bootstrap pointer so either agent can discover the channel.
